# YA_MCPServer_Template 开发指南

## 目录
1. [UV 包管理器介绍](#1-uv-包管理器介绍)
2. [UV 与 Conda 的关系](#2-uv-与-conda-的关系)
3. [UV 基础使用教程](#3-uv-基础使用教程)
4. [项目结构详解](#4-项目结构详解)
5. [开发流程指南](#5-开发流程指南)
6. [常见问题解答](#6-常见问题解答)

---

## 1. UV 包管理器介绍

### 什么是 UV？
UV 是一个**极速的 Python 包管理器和项目管理工具**，由 Rust 编写，是 pip 和 pip-tools 的现代替代品。

### 为什么使用 UV？
- ⚡ **速度快**：比 pip 快 10-100 倍
- 🔒 **依赖锁定**：自动生成 `uv.lock` 确保环境一致性
- 📦 **项目管理**：集成虚拟环境管理
- 🎯 **兼容性好**：完全兼容 `pyproject.toml` 和 `requirements.txt`

---

## 2. UV 与 Conda 的关系

### 会冲突吗？
**不会冲突！** UV 和 Conda 可以共存，但建议在同一个项目中只用一个。

### 主要区别

| 特性 | Conda | UV |
|------|-------|-----|
| 管理内容 | Python + 系统级依赖 | 仅 Python 包 |
| 速度 | 较慢 | 极快 |
| 环境创建 | `conda create` | `uv venv` |
| 安装包 | `conda install` | `uv pip install` |
| 锁文件 | environment.yml | uv.lock |
| 跨平台 | 优秀 | 优秀 |

### 使用建议
- **本项目要求**：使用 UV（老师要求）
- **你的环境**：可以保留 Conda base 环境，但在本项目中用 UV
- **切换方法**：每次进入项目时激活 UV 虚拟环境即可

---

## 3. UV 基础使用教程

### 3.1 安装 UV

**Windows PowerShell：**
```powershell
# 方式一：使用 pip 安装（推荐）
pip install uv

# 方式二：使用官方安装脚本
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

验证安装：
```powershell
uv --version
```

### 3.2 创建虚拟环境

```powershell
# 在项目根目录下创建虚拟环境（.venv 文件夹）
uv venv

# 指定 Python 版本（需要先安装对应 Python）
uv venv --python 3.10
```

### 3.3 激活虚拟环境

**Windows PowerShell：**
```powershell
# 激活虚拟环境
.\.venv\Scripts\Activate.ps1

# 如果遇到执行策略错误，运行：
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**激活后你会看到命令行前面有 `(.venv)` 标识**

### 3.4 安装依赖

```powershell
# 方式一：根据 pyproject.toml 安装所有依赖
uv pip sync

# 方式二：使用 uv 的智能安装
uv sync

# 方式三：安装单个包
uv pip install requests

# 查看已安装的包
uv pip list
```

### 3.5 常用命令对比

| 操作 | Conda 命令 | UV 命令 |
|------|-----------|---------|
| 创建环境 | `conda create -n myenv` | `uv venv` |
| 激活环境 | `conda activate myenv` | `.\.venv\Scripts\Activate.ps1` |
| 安装包 | `conda install numpy` | `uv pip install numpy` |
| 安装项目依赖 | `conda env create -f env.yml` | `uv sync` |
| 列出已安装包 | `conda list` | `uv pip list` |
| 删除环境 | `conda remove -n myenv --all` | 删除 `.venv` 文件夹 |
| 退出环境 | `conda deactivate` | `deactivate` |

### 3.6 锁定依赖版本

```powershell
# UV 会自动生成 uv.lock 文件，无需手动操作
# 这个文件记录了所有依赖的精确版本，确保环境一致性

# 如果需要更新依赖：
uv sync --upgrade
```

---

## 4. 项目结构详解

### 4.1 整体目录结构

```
YA_MCPServer_Template/
├── config.yaml              # 服务器配置文件
├── env.yaml                 # 加密的环境变量/密钥
├── pyproject.toml           # 项目元数据和依赖声明
├── uv.lock                  # 依赖锁定文件（自动生成）
├── server.py                # 主服务器入口
├── setup.py                 # 初始化设置
│
├── core/                    # 核心业务逻辑
│   ├── __init__.py
│   └── hello_secrets.py     # 示例：使用密钥
│
├── tools/                   # MCP Tools（工具函数）
│   ├── __init__.py
│   └── hello_tool.py        # 示例工具
│
├── resources/               # MCP Resources（数据资源）
│   ├── __init__.py
│   └── hello_resource.py    # 示例资源
│
├── prompts/                 # MCP Prompts（提示模板）
│   ├── __init__.py
│   └── hello_prompt.py      # 示例提示
│
├── modules/                 # 第三方模块（Git submodules）
│   ├── YA_Common/           # 通用工具库
│   └── YA_Secrets/          # 密钥管理工具
│
└── docs/                    # 文档
    ├── development-guides/
    └── prerequisites/
```

### 4.2 核心文件说明

#### `pyproject.toml` - 项目配置
```toml
[project]
name = "YA_MCPServer_Template"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.14.0",    # MCP 核心库
    "httpx>=0.28.1",       # HTTP 客户端
    "pyyaml>=6.0.2",       # YAML 解析
    # ... 其他依赖
]
```

#### `config.yaml` - 服务器配置
```yaml
server:
  name: YA_MCPServer_Template    # 服务器名称
  version: 0.1.0                  # 版本号
  
transport:
  type: "sse"                     # 传输方式：stdio 或 sse
  host: "127.0.0.1"              # SSE 模式下的监听地址
  port: 12345                     # SSE 模式下的端口

logging:
  console:
    enabled: true
    level: "DEBUG"
  file:
    enabled: true
    path: "logs/%Y-%m-%d_%H-%M-%S.log"
```

#### `env.yaml` - 加密的环境变量
使用 SOPS 加密的敏感信息（API 密钥等）

#### `server.py` - 主入口
```python
# 1. 初始化 FastMCP 服务器
app = FastMCP(server_name)

# 2. 注册三大组件
tools.register_tools(app)        # 注册工具
prompts.register_prompts(app)    # 注册提示
resources.register_resources(app) # 注册资源

# 3. 启动服务器（stdio 或 SSE 模式）
mcp_server.start()
```

### 4.3 MCP 三大组件

#### **Tools（工具）** - 让 AI 执行操作
```python
# tools/hello_tool.py
@app.tool()
def add_numbers(a: int, b: int) -> int:
    """将两个数字相加"""
    return a + b
```

**用途示例**：
- 查询数据库
- 调用外部 API
- 执行系统命令
- 文件操作

#### **Resources（资源）** - 提供数据给 AI
```python
# resources/hello_resource.py
@app.resource("file://config")
def get_config() -> str:
    """返回配置信息"""
    return "配置内容..."
```

**用途示例**：
- 提供文档内容
- 返回数据库数据
- 暴露系统状态

#### **Prompts（提示）** - 预定义的 AI 指令模板
```python
# prompts/hello_prompt.py
@app.prompt()
def greeting_prompt(name: str) -> str:
    """生成问候提示"""
    return f"请用友好的方式问候 {name}"
```

**用途示例**：
- 代码审查提示
- 数据分析模板
- 报告生成模板

---

## 5. 开发流程指南

### 5.1 环境准备（第一次）

```powershell
# 1. 进入项目目录
cd "d:\Syncthing Folder\Asus-Lenovo\School Projects\project_agent\YA_MCPServer_Template\YA_MCPServer_Template"

# 2. 创建虚拟环境
uv venv

# 3. 激活虚拟环境
.\.venv\Scripts\Activate.ps1

# 4. 安装依赖
uv sync

# 5. 初始化子模块（如果需要）
git submodule update --init --recursive
```

### 5.2 日常开发流程

#### 步骤 1：激活环境
```powershell
# 每次开始工作时
cd "项目目录"
.\.venv\Scripts\Activate.ps1
```

#### 步骤 2：开发新功能
假设你要开发一个"天气查询"工具：

**A. 创建工具文件**
```python
# tools/weather_tool.py
from modules.YA_Common.utils.logger import get_logger
import httpx

logger = get_logger(__name__)

def get_weather_tool(app):
    """注册天气查询工具"""
    
    @app.tool()
    def get_weather(city: str) -> dict:
        """
        查询指定城市的天气
        
        Args:
            city: 城市名称（如：北京、上海）
            
        Returns:
            包含天气信息的字典
        """
        try:
            # 这里调用天气 API
            logger.info(f"查询城市：{city}")
            
            # 示例返回
            return {
                "city": city,
                "temperature": "25°C",
                "condition": "晴天"
            }
        except Exception as e:
            logger.error(f"天气查询失败：{e}")
            raise
```

**B. 注册工具**
```python
# tools/__init__.py
from .hello_tool import get_hello_tool
from .weather_tool import get_weather_tool  # 新增

def register_tools(app):
    """注册所有工具"""
    get_hello_tool(app)
    get_weather_tool(app)  # 新增
```

#### 步骤 3：测试运行

```powershell
# 运行服务器（stdio 模式）
uv run python server.py

# 或者运行 SSE 模式（需要修改 config.yaml 中的 transport.type）
uv run python server.py
```

#### 步骤 4：调试
```powershell
# 查看日志文件
# 日志位置：logs/YYYY-MM-DD_HH-MM-SS.log

# 使用 Python 交互式测试
uv run python
>>> from tools.weather_tool import *
>>> # 测试你的函数
```

### 5.3 添加新依赖

```powershell
# 1. 使用 uv 安装包
uv pip install requests

# 2. 将依赖添加到 pyproject.toml
# 编辑 pyproject.toml，在 dependencies 列表中添加：
# "requests>=2.31.0"

# 3. 更新锁文件
uv sync
```

### 5.4 代码规范

项目使用了 `black` 和 `ruff` 进行代码格式化：

```powershell
# 格式化代码
uv run black .

# 检查代码规范
uv run ruff check .

# 自动修复
uv run ruff check --fix .
```

---

## 6. 常见问题解答

### Q1: 如何在 UV 和 Conda 之间切换？

```powershell
# 退出 Conda 环境
conda deactivate

# 激活 UV 环境
.\.venv\Scripts\Activate.ps1

# 反之亦然
deactivate
conda activate myenv
```

### Q2: UV 虚拟环境在哪里？
在项目根目录的 `.venv` 文件夹中（已在 `.gitignore` 中排除）

### Q3: 如何删除 UV 环境重新创建？
```powershell
# 1. 退出环境
deactivate

# 2. 删除 .venv 文件夹
Remove-Item -Recurse -Force .venv

# 3. 重新创建
uv venv
.\.venv\Scripts\Activate.ps1
uv sync
```

### Q4: uv.lock 是什么？需要提交到 Git 吗？
- `uv.lock` 是依赖锁定文件，记录所有包的精确版本
- **本项目已在 `.gitignore` 中排除**，不提交到 Git
- 每个开发者根据 `pyproject.toml` 自己生成

### Q5: 如何使用密钥（env.yaml）？
```python
# 在代码中使用
from core.hello_secrets import get_secret

api_key = get_secret("api_key")
```

### Q6: Transport 的 stdio 和 SSE 有什么区别？
- **stdio**：通过标准输入输出通信（适合 Claude Desktop 等客户端）
- **sse**：通过 HTTP Server-Sent Events 通信（适合 Web 应用）

### Q7: 如何查看 MCP 服务器是否正常运行？

**stdio 模式**：
```powershell
uv run python server.py
# 应该看到服务器启动信息，然后等待输入
```

**SSE 模式**：
```powershell
uv run python server.py
# 访问 http://127.0.0.1:12345/ 应该能建立 SSE 连接
```

### Q8: 子模块（YA_Common、YA_Secrets）如何更新？
```powershell
# 更新所有子模块
git submodule update --remote --merge

# 只更新特定子模块
git submodule update --remote modules/YA_Common
```

---

## 快速开始检查清单

- [ ] 安装 UV：`pip install uv`
- [ ] 创建虚拟环境：`uv venv`
- [ ] 激活环境：`.\.venv\Scripts\Activate.ps1`
- [ ] 安装依赖：`uv sync`
- [ ] 运行服务器：`uv run python server.py`
- [ ] 查看日志：检查 `logs/` 文件夹
- [ ] 开发新功能：在 `tools/`、`resources/` 或 `prompts/` 添加文件
- [ ] 注册功能：在对应的 `__init__.py` 中注册
- [ ] 测试：重新运行 `uv run python server.py`

---

## 推荐学习资源

- UV 官方文档：https://docs.astral.sh/uv/
- MCP 官方文档：https://modelcontextprotocol.io/
- FastMCP 文档：https://github.com/jlowin/fastmcp
- 项目内文档：`docs/development-guides/`

---

## 需要帮助？

1. 查看项目日志：`logs/` 文件夹
2. 查看文档：`docs/` 文件夹
3. 咨询老师或同学
4. 搜索相关错误信息

---

**祝开发顺利！** 🚀
